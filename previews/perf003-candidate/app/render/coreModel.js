/** Compatibility entry point. Canonical command queries stay in the trusted host adapter.
 * SVG/UI consumers receive only the detached projected BrowserRenderModel. */
export * from '../core-adapter/browserProjection.js';
